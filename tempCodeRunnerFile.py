#detect collision with wall
    # if ball.ycor() > (SCREEN_HEIGHT-20) or ball.ycor() < -(SCREEN_HEIGHT-20):
    #     ball.bounce_y()
    # #detect collision with paddle
    # if ball.distance(paddleSx) < 50 and ball.xcor() < -320 or ball.distance(paddleDx) < 50 and ball.xcor() > 320:
    #     ball.bounce_x()